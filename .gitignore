node_modules/
dist/
.DS_Store
*.tfvars
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Allow test fixture state files (contain only fake IDs)
!test-data/**/source.tfstate

# Local agent working files (brainstorm sessions)
.superpowers/

# Git worktrees
.worktrees/
.claude/usage-log.txt

# One-off rate limit capture output (review, then merge into src/shared/rate-limit-baselines.json)
rate-limit-capture.json
